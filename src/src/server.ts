/**
 * @deprecated v5.2 — CÓDIGO MORTO. Removido na v5.2.
 * Os entry points são stdio.ts (STDIO/IDE) e index.ts (HTTP/SSE).
 * Este arquivo existe apenas para evitar erros de compilação em referências residuais.
 */
export {};
